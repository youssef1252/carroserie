const url = `http://localhost:3000`;
export const config = {
  apiUrl: url,
  login: `${url}/auth/login`,
  users: `${url}/users`,
  userAdd: `${url}/auth/register`,
  user: `${url}/user`,
  marks: `${url}/marks`,
  modelesByMark: `${url}/modeles`
};
